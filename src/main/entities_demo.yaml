# Fallbacks if a key is missing from m5-dial.yaml. Hidden on the Dial.
substitutions:
  climate_entity: climate.disabled
  music_player_entity: media_player.disabled
  timer_entity: timer.disabled
  light_entity: light.disabled
