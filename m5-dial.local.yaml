# Développement uniquement : compile CE clone (components/ + src/) sans GitHub.
# Le dashboard ESPHome utilise m5-dial.yaml (package GitHub), pas ce fichier.

substitutions:
  device_name: m5stack-dial
  device_friendly_name: M5Stack Dial
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  api_encryption_key: !secret api_encryption_key
  ota_password: !secret ota_password
  ap_ssid: Dial Fallback Hotspot
  ap_password: "changeme-dial-ap"

  timezone: Europe/Paris
  ui_language: fr
  encoder_resolution: "1"
  weather_entity: weather.disabled
  aqi_entity: sensor.disabled
  timer_entity: timer.disabled

  # Local files from this clone (empty prefix).
  asset_url: ""

# Local components while iterating; m5-dial.yaml (GitHub package) uses github:// instead.
external_components:
  - source:
      type: local
      path: components
    components: [dial_lights, dial_ha_list, dial_climates, dial_media_players, dial_covers, dial_garages, dial_switches, dial_scenes, dial_temperatures, dial_carousel, dial_i18n, generic_image, media_source, sendspin]

packages:
  firmware: !include dial.yaml
